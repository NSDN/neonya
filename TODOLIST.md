# Todolist

1. 研究一下请求帖子列表的路由是否有更佳写法（目前为 `thread?boardid=x`）。
2. 实现后端 GET /api/threads/:id — 目前无法查看单个帖子内容。
3. 实现帖子详情页 — 前端路由 /threads/:id + 组件，引用最多的 TODO。
4. 更改注册登入画面 UI。
5. 实现前端发帖 UI — 后端 API 已就绪，只差前端表单。
